# Launch tweet (user-typed at Gate 0)

```
your week, itemized 🧾

[image: example.png]

built as a claude skill, runs locally, your data never leaves your laptop — npx github:KeWang0622/vibereceipt
```

Pick a 1200×675 export of `assets/example.svg` as the attached image. The full SVG is taller (5:7-ish receipt aspect), so crop or letterbox to fit X's preview.
